# Production build config (read automatically by `vite build`).
# The admin panel is served same-origin by nginx, which proxies /api → backend,
# so the API base is relative — no CORS and no hardcoded host.
VITE_API_BASE_URL=/api/v1
