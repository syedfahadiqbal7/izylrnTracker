import { PlaceholderPage } from "@/components/PlaceholderPage";

export function RosterPage() {
  return (
    <PlaceholderPage
      title="Roster"
      description="Enrolled students and parent-consent status."
      planned="Coming soon: roster table (GET /schools/students) with enroll + remove."
    />
  );
}
