import { PlaceholderPage } from "@/components/PlaceholderPage";

export function AttendancePage() {
  return (
    <PlaceholderPage
      title="Attendance"
      description="Daily register — every consented student's status for a date."
      planned="Coming next: daily register table (GET /schools/attendance) with manual override."
    />
  );
}
