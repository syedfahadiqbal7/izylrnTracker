import { PlaceholderPage } from "@/components/PlaceholderPage";

export function AuditPage() {
  return (
    <PlaceholderPage
      title="Audit Log"
      description="The school's audit trail (admin only)."
      planned="Coming soon: filterable, paginated audit log (GET /schools/audit)."
    />
  );
}
